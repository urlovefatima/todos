package sn.ept.git.seminaire.cicd.entities;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class BaseEntityTest {
    @Test
    void testConstructor() {
        // BaseEntity est abstraite, on ne peut pas l'instancier directement
        // On peut tester via une sous-classe concrète si besoin
        assertTrue(true);
    }
    // Ajouter ici des tests pour les méthodes communes si besoin
} 